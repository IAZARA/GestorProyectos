const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const notifications = await prisma.notification.findMany(); console.log("Notificaciones en la base de datos:"); notifications.forEach(notification => console.log(`ID: ${notification.id}, Tipo: ${notification.type}, De: ${notification.fromId}, Para: ${notification.toId}, Leída: ${notification.isRead}`)); await prisma.$disconnect(); } main().catch(e => { console.error(e); process.exit(1); });
